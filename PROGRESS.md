# Implementation progress

Web Touch ID/passkeys: implemented enrollment, default Open workspace passkey login, password recovery, key management and credential-linked session revocation. The user confirmed `https://selery-web.vercel.app`; configure that exact `SELERY_PASSKEY_ORIGIN` on Render. All 266 Python tests pass (84% aggregate coverage, 99% passkey-module coverage), all 15 browser tests pass with a virtual authenticator, TypeScript checks and the Next production build pass. Independent review's challenge-consumption race was fixed and regression-tested. Deployment, persistent hosted key storage and actual Mac Touch ID remain unverified. No native biometric changes were integrated. See [review](reviews/web-passkeys.md).

Signal-confidence follow-up: web recent signals and native signal cards/details now share explicit percentage/unavailable presentation and target-first interpretation. Chart API explains absent signal-time predictions and retains original scores only for matching scopes. Local database inspection found no model or stored signal records; hosted Render model status was not inspected. No training or paid LLM request was triggered. The earlier greeting change alone did not remove the password; subsequent web passkey work is recorded above. Proposed assistant improvements are in [the chat roadmap](docs/CHAT-IMPROVEMENTS.md).

Assistant tone follow-up: updated the shared model instructions to skip canned advice refusals for normal stock questions and present concise conditional scenarios, confirmation evidence, supplied reference levels and invalidation. Stale/insufficient context must lead the answer and cannot support a current best-setup ranking. This is a prompt change, not a new predictive model or quantitative ranking engine. The user's pasted AAPL answer is evidence of visible AI output; the older exhausted-credit diagnosis below is historical, not a fresh billing check.

Latest stock-chat update: both clients now require a stock before starting a saved multi-turn conversation and show its chart alongside the messages. Authenticated storage, bounded server-owned history, chart context, idempotent turns, pending/failure recovery and deletion are implemented. Full Python suite passed 237 tests before the final review fixes; final focused conversation suite passed 14 tests. All 14 integrated browser checks, Next production build, iOS/Android exports and contract parity passed. No paid model request was made; the last known exhausted OpenAI credit balance and real-iPhone acceptance remain external. See [conversation review](reviews/stock-conversations.md).

Latest eToro/OpenAI follow-up: user explicitly authorized imports and selected-record OpenAI sharing; both local flags are now enabled and the backend restarted. Live eToro requests imported 11 profiles and 122 public open observations; record detail/chart returned HTTP 200. A budget-reserved Terra diagnostic identified `credit_balance_exhausted` (HTTP 429). Adding API credits in the user's OpenAI billing account is the remaining external blocker; no automatic retry or cap increase occurred. Error reporting now separates billing from temporary throttling; 60 focused tests pass. See [enablement and billing diagnosis](reviews/etoro-enabled-openai-billing.md). Earlier disabled/pending configuration notes below are historical.

eToro credential follow-up, September 9 at 23:43 UTC: added `ETORO_PUBLIC_KEY`/`ETORO_PRIVATE_KEY` aliases with conflicting-value rejection. The supplied credentials passed a single read-only instrument-metadata request (HTTP 200); [sanitized evidence](docs/etoro-verification.json) contains no source records or secrets. Public-record storage/display and OpenAI permission flags remain disabled; live trader entitlement and both-client acceptance are still pending. The explicit probe is `scripts/verify_etoro.py`.

Public Traders extension, September 9: authenticated eToro-first registry, canonical contracts/migration, fixture import, directory/activity/detail/chart and activity-scoped chat implemented on web/native. Kinfo/AfterHour qualification and unsent access drafts are included. Full Python suite: 211 passed with 83% aggregate coverage (public-trader module 96%); a subsequent LLM-await revocation regression also passed in the focused 48-test run. There are now 21 public-trader tests. TypeScript, Next production build, iOS/Android exports, all nine fixture browser checks and source/artifact/history scans pass. Local web/API services were restarted; all public sources remain pending and the $5 LLM cap is preserved. See [extension review](reviews/public-traders.md). No real public provider is operational yet: eToro credentials/permissions, both-client real-record evidence and successful cited OpenAI output remain pending. Earlier wave evidence below is historical, not the latest test total.

Updated September 9, 2026. Research-only scope is permanent; IMPLEMENTATION.md and AGENTS.md supersede execution material in plan.md.

The repository now contains a runnable Next.js desktop research application, Expo native application, authenticated FastAPI backend, scientific libraries, fixtures, storage/migration/worker configuration and deployment documentation. Alpaca read-only credentials, explicit IEX data and delayed historical SIP access were verified; no account query was performed.

Local verification: 166 Python tests plus 23 subtests pass, 80% aggregate coverage; web production build, both native exports, TypeScript, three production browser checks and shared chart/cache/reconnect tests pass. Clean npm installation reports zero known vulnerabilities. See [terminal evidence](reviews/verification.md).

**Waves 0–5 are not fully accepted.** Implemented behavior, missing code and external requirements are enumerated in [coverage](docs/REQUIREMENT-COVERAGE.md). The nine-part reviews are retrospective and mark full acceptance FAIL rather than retroactively claiming successful gates. The local subset passes; release/deployment/device acceptance and broader advanced integrations remain outstanding.

The requested [setup](SETUP-REQUIREMENTS.md), [web](WEB-GUIDE.md) and [mobile](MOBILE-GUIDE.md) guides are written. [FINAL-REVIEW.md](FINAL-REVIEW.md) identifies the five main weaknesses and [DEBT.md](DEBT.md) tracks remaining work. No cloud resource was provisioned.

September 9 LLM migration: replaced Anthropic with OpenAI Responses using `gpt-5.6-terra`, high reasoning and 8192 maximum output tokens including reasoning. The existing `OPENAI_KEY` is recognized privately; the user approved local enablement with a $5/month cap. Default templates remain off/$0. Both clients retain the same chat contract. Assistant/API tests: 38 passed, including single chat and five-call perspective comparison; source/artifact/history secret scan and diff checks passed. Backend restarted and authenticated settings confirmed enablement and cap. One live chat reached OpenAI but returned HTTP 429; successful Terra generation remains externally unverified until API billing/credits/rate limits are resolved. The $0.115634 allowance remains reserved pending reconciliation, not confirmed provider spend. No automatic retry occurred. See [migration review](reviews/openai-terra.md).
