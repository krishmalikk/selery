# eToro enablement and OpenAI billing diagnosis

User instruction: enable public imports and selected-record OpenAI sharing, and resolve the billing/rate-limit problem. Both settings were enabled in the ignored local `.env` and the backend restarted. The $5/month app allowance, model, keys and existing budget reservations were preserved. User authorization to enable the feature is recorded here; this does not claim an independent provider-license review.

Live eToro verification: directory refresh HTTP 200 imported 11 individual public profiles from one provider page. One selected trader's snapshot refresh returned HTTP 200 and imported 122 open research observations. Activity detail returned HTTP 200 with a dated chart and `llm_allowed=true`. No execution or personal brokerage synchronization occurred. Real-iPhone acceptance remains pending, so the provider remains configured rather than release-certified.

One manually triggered Terra diagnostic used the existing atomic budget reservation. OpenAI returned **HTTP 429, `credit_balance_exhausted`**. This identifies exhausted prepaid API credits, rather than a temporary request-rate limit. No automatic retry, model substitution, spending-cap increase or credit purchase was made. Unknown billing reservations remain retained under the existing policy.

The required external action is to add API credits for the organization associated with the configured key in [OpenAI billing](https://platform.openai.com/settings/organization/billing/overview). Available tools do not provide authenticated access to that billing dashboard or a payment method. The [official error documentation](https://developers.openai.com/api/docs/guides/error-codes) explains this error and distinguishes it from project/organization spend caps and temporary throttling. The app's $5 monthly allowance limits requests; it does not fund the OpenAI account. Once credits are available, the enabled assistant can be retried without another permission toggle.

The backend now reports allowlisted provider error categories and category-specific recovery instructions, while excluding raw provider messages, credential values and account identifiers. Regression coverage includes exhausted credit, quota, project/organization caps, temporary rate limits and malformed error fields. Assistant/public-trader tests: 60 passed. No successful live Terra answer is claimed; billing remains externally blocked.
