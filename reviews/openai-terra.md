# OpenAI Terra migration review — September 9, 2026

1. **Scope:** requested provider/model switch is implemented in the shared backend. Research-only restrictions, manual invocation and client contracts remain intact.
2. **Architecture:** fixed OpenAI Responses endpoint, bearer credential held only on the backend, `store=false`, no tools. Default model is `gpt-5.6-terra`, reasoning high, total output ceiling 8192 tokens.
3. **Security:** key aliases are supported without logging values; conflicting keys fail explicitly and dataclass repr hides the key. Provider error bodies are withheld. Source, existing client artifacts and Git history scanned without credential findings.
4. **Budget correctness:** atomic pre-request reservations, default off/$0, user-approved local $5 cap, finite positive prices, reasoning billed once as output. Cached inputs use a conservative full-price allowance. Unknown billing retains reservations; excess usage enables the persistent kill switch.
5. **Feed and research honesty:** evidence prompt preserves IEX limitations and unavailable-data rules. No new data or execution capabilities are introduced.
6. **Application behavior:** Responses output traversal ignores reasoning items, handles text/refusals, and reports incomplete or empty results without silent success. Valid reported usage is recorded even if no complete answer is available.
7. **Verification:** 38 focused assistant/API tests passed, including authenticated single chat and manual five-call synthesis; two pre-existing dependency deprecation warnings. Diff checks and scan of 196 authored files plus 172 existing build artifacts passed before this review was added. No client code/contract changes required a rebuild.
8. **External evidence:** local backend restart and authenticated settings passed. One live request reached OpenAI and returned HTTP 429. No successful generation is claimed; account billing, credits or rate limits must be checked. No automatic retry. The $0.115634 budget reservation is not evidence of actual provider charges.
9. **Decision and remaining limitations:** implementation PASS for the scoped migration; live provider acceptance PENDING. The existing 45-second provider timeout and 285-second web proxy window may limit complex high-reasoning or five-call workflows. No queued long-job change is claimed. Reconcile unknown charges before resetting reservations; resolve provider access and verify success before declaring the assistant operational.
