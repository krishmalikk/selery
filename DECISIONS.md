# Decisions

The user authorized implementing and deploying docs/CHAT-IMPROVEMENTS.md. Use bounded deterministic context selection and existing OpenAI budget enforcement. Stream only visible model text, with durable pending rows and read-only client polling so both clients can recover without duplicate requests. Stop means stop display, not confirmed billing cancellation. Summaries are explicit extractive history aids, never current evidence. Signal-bound threads preserve one server-selected snapshot and chart interval; unrelated evidence is dated separately. Deploy existing Vercel/Render services through the GitHub repository; no extra service or model was authorized or purchased.

The user selected Touch ID for the web app, with `https://selery-web.vercel.app` as its origin. Use standard WebAuthn platform passkeys with required user verification; the OS chooses biometric or device-unlock fallback. Initial enrollment requires the workspace password and an authenticated session. Keep password recovery and private sessions. No Google/email identity service, biometric secret, or native biometric implementation is needed. Exact origin and RP scope fail closed, IP-based origins are rejected, public keys persist in the existing database, and one API process owns bounded one-use challenges. Browser verification uses a virtual authenticator; it does not count as Mac hardware or hosted acceptance.

Signal confidence displays the original Python model's calibrated target-first estimate for the stated horizon/feed, never indicator agreement or an LLM's self-reported certainty. Missing predictions remain unavailable with a reason. Historical signals are not retroactively assigned a newer model's probability. Calibrated and unscored views share presentation helpers across clients.

Assistant tone follow-up: ordinary stock/setup questions should lead with evidence-backed conditional analysis, without generic advice refusals. The research-only scope and persistent disclaimer remain. Qualitative setup comparisons must identify their evidence criteria; stale or insufficient data cannot support a current best-setup claim. Python remains the source of calculations and derived levels. No fabricated targets, calibrated probabilities or personal execution instructions are introduced. Prompt changes follow [official OpenAI guidance](https://developers.openai.com/api/docs/guides/prompt-engineering); live wording still needs an on-request model evaluation.

Stock conversation UX replaces the generic one-answer assistant panel. A stock is explicitly selected before chat and is immutable for that thread. Saved messages live in the existing private backend database; they are separate from journal entries and public-trader ephemeral evidence. Follow-up model requests use server-built role history and fresh selected-timeframe IEX context, not client-submitted history or provider-hosted conversation storage. Up to ten pairs/16KB of prior text are replayed; long latest exchanges retain marked excerpts. Paid requests remain explicit, budget-reserved, and idempotent; no automatic retry is added.

The user subsequently explicitly authorized eToro imports and selected-record OpenAI sharing. Both local flags are enabled; the original opt-in template defaults remain false. The diagnosed OpenAI `credit_balance_exhausted` error requires account funding, not a model change or a larger app budget. Keep the $5 cap, do not retry quota failures automatically, and expose only known error categories. [Evidence](reviews/etoro-enabled-openai-billing.md).

Public Traders extension: third-party published research observations are permitted under the accepted user plan, independently of the continuing prohibition on the user's broker-state synchronization or execution. Public identities remain source-specific; only eToro has a documentation-based adapter, and real contracts/entitlements are provisional. Kinfo/AfterHour require authorized feeds, with inquiries drafted but unsent. No cross-provider return is computed until instrument/currency comparability is verified. Missing open records stay unconfirmed; access withdrawal purges current and revision payloads when detected. Source LLM permission is separate from storage/display permission and the existing $5 monthly cap. Shared-contract changes were integrated centrally; web/mobile work used isolated worktrees. See [design and access qualification](docs/PUBLIC-TRADERS.md).

1. The final user correction removes all execution and broker-state capabilities. Python owns all quantitative calculations. Both clients share generated contracts and chart rendering; they never compute strategies.
2. Alpaca credentials retain ALPACA_ENDPOINT, ALPACA_KEY, ALPACA_SECRET. A strict HTTPS paper-host check precedes market-data requests. No account request was made. Read-only historical IEX/SIP and news requests succeeded; evidence is in docs/alpaca-verification.json.
3. Fixture mode is the local default for deterministic development. Fixtures are real recorded data and labeled stale; the options fixture is explicitly synthetic. Live mode is an explicit environment setting, never a silent fallback.
4. SQLite is supported for a zero-service local start. PostgreSQL/TimescaleDB migrations and Compose are the target durable deployment. Redis/Arq is required for the separate job worker, not the local thin slice.
5. A single Python API process owns local sessions/stream tickets and the forward observer. Horizontal replicas require shared auth/observer leadership before deployment. Default server scale is one.
6. Shared WebView/desktop chart renderer uses Lightweight Charts with required attribution. No external chart CDN or key enters mobile. iPhone gesture/performance acceptance remains external evidence.
7. The cost model has verified effective-date coverage through 2026-09-09, Decimal arithmetic, explicit spread/slippage/impact assumptions, and no IEX-volume liquidity estimate. Unsupported dates fail instead of extrapolating regulatory schedules.
8. Sources are pinned and classified; unavailable pandas-ta is replaced by audited pandas-ta-classic. Restrictive/unlicensed source is not copied. Dependency locks are distinct from the repository inspiration audit.
9. LLM use starts disabled at a zero budget. The local assistant exposes data summaries with timestamps and refuses causal news claims. No scheduled expensive jobs.

10. Full SIP capability flags now distinguish implemented functions from merely eligible data. Bar-aggregated typical-price VWAP is a historical approximation, not tick-exact VWAP; IEX remains disabled.
11. Missing regular-session coverage fails conservatively; ordinary weekends/DST are handled, but unverified holidays/early closes are not inferred. Annualized ratios are withheld on unexplained weekday gaps.
12. Forward confidence is captured at signal time and reused from immutable snapshots; historical bars never receive a later-trained model. SHAP remains explicit-request only.
13. Python optional model libraries were installed and tested locally. A modern Numba constraint and macOS OpenMP dependency were necessary. Torch sequence compute uses one CPU thread after a mixed-indicator/model regression exposed a native crash.
14. Fresh npm resolution uses a small original bounded CommonJS URI decoder in place of a vulnerable transitive decoder, plus compatible UUID/PostCSS overrides. Malformed encodings remain unchanged; valid decoding is tested. Final audit reports zero vulnerabilities.
15. The measured 255 MiB fixture API profile excludes databases/workers/training/cloud overhead; it does not justify a hosting-cost increase. All paid integrations remain off.
16. Retrospective wave reviews are marked FAIL for full acceptance where requirements are partial. This records the gate-process shortfall rather than claiming earlier complete PASS results. The three user guides describe the implemented subset and remaining dependencies.
17. On September 9, 2026 the user selected OpenAI GPT-5.6 Terra and approved a $5 monthly LLM cap. The backend uses Responses with high reasoning, no tools, store=false, and an 8192-token output ceiling including reasoning. OPENAI_KEY is accepted alongside canonical OPENAI_API_KEY. Committed defaults remain off/$0; local enablement is explicit. Existing atomic reservations and durable kill switch remain authoritative; cached inputs are budgeted conservatively at full rate.
